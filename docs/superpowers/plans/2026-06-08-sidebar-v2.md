# Sidebar v2 — 5 Pilastri Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refactoring radicale della sidebar da 25+ voci a 5 macro-pilastri operativi con navigazione condizionale (geo-flag PA Italy, GPAI detection, NIST opt-in) e indicatore di progresso DocuGen.

**Architecture:** Tre cambiamenti ortogonali: (1) `OrgProfile` come schema condiviso in localStorage per flag organizzativi; (2) sidebar `layout.tsx` riscritta con pilastri accordion a 2 livelli e filtro condizionale; (3) progress tracker in DocuGen con gate sul bottone PDF. Il refactoring è retrocompatibile — le route esistenti non vengono spostate.

**Tech Stack:** Next.js 16 App Router · TypeScript · Lucide React · localStorage (pattern esistente via `storage-schema.ts`)

---

## File map

| File | Azione | Responsabilità |
|------|--------|----------------|
| `src/lib/dossier/storage-schema.ts` | Modifica | Aggiunge `OrgProfile` type + chiave `orgProfile` in `STORAGE_KEYS` |
| `src/lib/hooks/useOrgProfile.ts` | Crea | Hook React che legge/scrive `OrgProfile` da localStorage |
| `src/app/dashboard/triage/page.tsx` | Modifica | Quando riskTier==="gpai" scrive `gpaiDetected: true` in OrgProfile |
| `src/app/dashboard/layout.tsx` | Modifica | Riscrive navGroups → 5 pilastri con accordion + flag condizionali |
| `src/app/dashboard/account/page.tsx` | Modifica | Aggiunge sezione Settings con toggle PA Italy e NIST opt-in |
| `src/app/dashboard/tools/docugen/page.tsx` | Modifica | Aggiunge `DocuGenStepTracker` component + gate PDF |

---

## Task 1: OrgProfile in storage-schema + hook

**Files:**
- Modify: `src/lib/dossier/storage-schema.ts` (aggiungere dopo l'ultima interfaccia esistente)
- Create: `src/lib/hooks/useOrgProfile.ts`

- [ ] **Step 1: Aggiungi `OrgProfile` a storage-schema.ts**

Cerca la riga con `export const STORAGE_KEYS` nel file e aggiungi prima di essa:

```typescript
// ── Org Profile — flag organizzativi per sidebar condizionale ───────────────

export interface OrgProfile {
  /** True se l'organizzazione opera con la PA italiana → mostra L.132 + AGID/ACN */
  paItaly: boolean;
  /** True se il sistema classificato in Triage è un modello GPAI → mostra GPAI Assessment */
  gpaiDetected: boolean;
  /** True se l'utente ha abilitato il mapping NIST AI RMF (Enterprise opt-in) */
  nistEnabled: boolean;
  /** Nome organizzazione (opzionale — per personalizzazione UI) */
  orgName?: string;
}

export const DEFAULT_ORG_PROFILE: OrgProfile = {
  paItaly: false,
  gpaiDetected: false,
  nistEnabled: false,
};
```

E nella stessa export `STORAGE_KEYS`, aggiungi la voce `orgProfile`:

```typescript
  orgProfile: "aicomply_org_profile",
```

- [ ] **Step 2: Verifica TypeScript**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && npx tsc --noEmit 2>&1 | grep "storage-schema" | head -5
```
Expected: nessun output

- [ ] **Step 3: Crea `src/lib/hooks/useOrgProfile.ts`**

```typescript
// src/lib/hooks/useOrgProfile.ts
"use client";

import { useState, useEffect, useCallback } from "react";
import {
  readFromStorage,
  writeToStorage,
  OrgProfile,
  DEFAULT_ORG_PROFILE,
} from "@/lib/dossier/storage-schema";

export function useOrgProfile() {
  const [profile, setProfileState] = useState<OrgProfile>(DEFAULT_ORG_PROFILE);

  useEffect(() => {
    const stored = readFromStorage<OrgProfile>("orgProfile");
    if (stored) setProfileState(stored);
  }, []);

  const setProfile = useCallback((updates: Partial<OrgProfile>) => {
    setProfileState((prev) => {
      const next = { ...prev, ...updates };
      writeToStorage<OrgProfile>("orgProfile", next);
      return next;
    });
  }, []);

  return { profile, setProfile };
}
```

- [ ] **Step 4: Verifica TypeScript**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -10
```
Expected: nessun output

- [ ] **Step 5: Commit**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && git add src/lib/dossier/storage-schema.ts src/lib/hooks/useOrgProfile.ts && git commit -m "feat: OrgProfile type + useOrgProfile hook per flag condizionali sidebar"
```

---

## Task 2: Triage — scrivi gpaiDetected in OrgProfile

**Files:**
- Modify: `src/app/dashboard/triage/page.tsx` (funzione `syncToClassifier`)

Il triage scrive già in `STORAGE_KEYS.classifier` tramite `syncToClassifier`. Dobbiamo anche scrivere in `OrgProfile.gpaiDetected` quando il tier è `"gpai"`.

- [ ] **Step 1: Aggiungi import in cima a triage/page.tsx**

Individua gli import esistenti (cercano `writeToStorage`) e aggiungi:

```typescript
import type { OrgProfile } from "@/lib/dossier/storage-schema";
```

(Già importa `writeToStorage` e `readFromStorage`, quindi aggiungi solo il tipo)

- [ ] **Step 2: Modifica `syncToClassifier` per settare gpaiDetected**

Alla fine della funzione `syncToClassifier`, subito prima della `}` di chiusura del `try`, aggiungi:

```typescript
        // Aggiorna gpaiDetected in OrgProfile se il tier è GPAI
        if (r.riskTier === "gpai") {
          const orgProfile = readFromStorage<OrgProfile>("orgProfile") ?? {
            paItaly: false, gpaiDetected: false, nistEnabled: false
          };
          writeToStorage<OrgProfile>("orgProfile", { ...orgProfile, gpaiDetected: true });
        }
```

- [ ] **Step 3: Verifica TypeScript**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && npx tsc --noEmit 2>&1 | grep "triage" | head -5
```
Expected: nessun output

- [ ] **Step 4: Commit**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && git add src/app/dashboard/triage/page.tsx && git commit -m "feat: triage scrive gpaiDetected=true in OrgProfile quando tier=gpai"
```

---

## Task 3: Sidebar v2 — 5 pilastri con accordion + flag condizionali

**Files:**
- Modify: `src/app/dashboard/layout.tsx` (sostituzione completa di navGroups + accordion logic)

Questo è il task principale. Il layout attuale usa un array piatto di `NavGroup[]` (gruppo label → items). La nuova struttura usa `NavPillar[]` con un livello opzionale di `children`.

- [ ] **Step 1: Sostituisci l'intera sezione navGroups e aggiungi tipi**

Sostituisci dal `type NavItem` fino alla fine di `navGroups` (righe 24–132) con il codice seguente. **ATTENZIONE**: mantieni tutto il codice dopo riga 132 (la funzione `DashboardLayout`) invariato tranne il blocco di rendering della nav.

```typescript
type NavChild = {
  icon: React.FC<React.SVGProps<SVGSVGElement>>;
  label: string;
  href: string;
  art?: string;
  badge?: "urgent" | "new";
  /** se presente, la voce si mostra solo se il flag corrispondente in OrgProfile è true */
  flag?: "paItaly" | "gpaiDetected" | "nistEnabled";
};

type NavPillar = {
  id: string;
  icon: React.FC<React.SVGProps<SVGSVGElement>>;
  label: string;
  art?: string;
  badge?: "urgent" | "new";
  /** se href, click naviga; se children, click espande l'accordion */
  href?: string;
  children?: NavChild[];
};

// ── 5 Pilastri operativi ─────────────────────────────────────────────────────
// Voci rimosse dalla sidebar (non eliminate, spostano dove indicato):
//   - AIA Architect         → background script, non più voce nav
//   - Transparency/Oversight/Resilience/Art.50 Kit → step interni DocuGen
//   - GPAI Module separato  → unificato come step interno DocuGen (flag gpaiDetected)
//   - NIST AI RMF           → hidden by default, opt-in in Settings
//   - L.132/2025, AGID/ACN  → condizionali (flag paItaly) sotto Compliance Ops
const PILLARS: NavPillar[] = [
  {
    id: "triage",
    icon: Crosshair,
    label: "Triage",
    href: "/dashboard/triage",
    art: "Art. 5/6/51",
    badge: "urgent",
  },
  {
    id: "risk",
    icon: ShieldAlert,
    label: "Risk Manager",
    art: "Art. 9/27/35",
    children: [
      { icon: Zap,           label: "Risk Register",  href: "/dashboard/tools/risk-manager", art: "Art. 9"  },
      { icon: BookMarked,    label: "FRIA",           href: "/dashboard/tools/fria",         art: "Art. 27" },
      { icon: ShieldAlert,   label: "DPIA",           href: "/dashboard/tools/dpia",         art: "Art. 35" },
    ],
  },
  {
    id: "docugen",
    icon: FileCode,
    label: "DocuGen AI",
    href: "/dashboard/tools/docugen",
    art: "All. IV",
    badge: "new",
  },
  {
    id: "data-audit",
    icon: ClipboardList,
    label: "Data Audit",
    href: "/dashboard/tools/data-audit",
    art: "Art. 10",
  },
  {
    id: "compliance",
    icon: Scale,
    label: "Compliance Ops",
    art: "Art. 12–72",
    children: [
      { icon: FileArchive,    label: "LogVault",            href: "/dashboard/tools/logvault",            art: "Art. 12" },
      { icon: TrendingUp,     label: "Post-Market",         href: "/dashboard/post-market",               art: "Art. 72" },
      { icon: Database,       label: "EUDB Registration",   href: "/dashboard/tools/eudb",                art: "Art. 49" },
      { icon: UserCheck,      label: "Authorized Rep.",     href: "/dashboard/tools/authorized-rep",      art: "Art. 22" },
      { icon: ArrowRightLeft, label: "Provider Transition", href: "/dashboard/tools/provider-transition", art: "Art. 28" },
      { icon: Map,            label: "Deadline Timeline",   href: "/dashboard/journey",                   art: ""        },
      { icon: Building2,      label: "Trust Center",        href: "/dashboard/trust-center",              art: ""        },
      { icon: Scale,          label: "L.132/2025",          href: "/dashboard/tools/l132",                art: "PA Italy", flag: "paItaly" },
      { icon: Landmark,       label: "AGID/ACN",            href: "/dashboard/tools/agid-acn",            art: "PA Italy", flag: "paItaly" },
    ],
  },
];
```

- [ ] **Step 2: Aggiorna gli import Lucide nella riga 5**

Sostituisci la riga degli import lucide con:

```typescript
import {
  FileCode, Scale, ShieldAlert, BookMarked, ClipboardList, Crosshair,
  FileArchive, TrendingUp, Database, UserCheck, ArrowRightLeft, Map, Building2,
  Landmark, Zap, Menu, X, ChevronRight, ChevronLeft, ChevronDown,
  LogOut, Settings,
} from "lucide-react";
```

- [ ] **Step 3: Aggiorna gli import del hook nella riga 13**

Aggiungi l'import `useOrgProfile` dopo gli import React esistenti:

```typescript
import { useOrgProfile } from "@/lib/hooks/useOrgProfile";
```

- [ ] **Step 4: Aggiungi state accordion + useOrgProfile in DashboardLayout**

All'interno di `DashboardLayout`, subito dopo `const { role } = useUserRole();`, aggiungi:

```typescript
  const { profile: orgProfile } = useOrgProfile();

  // Pilastri con children auto-espansi se la route corrente è un loro figlio
  const [expandedPillars, setExpandedPillars] = useState<Set<string>>(() => {
    const set = new Set<string>();
    // Auto-espandi il pilastro che contiene la route attiva
    for (const pillar of PILLARS) {
      if (pillar.children?.some((c) => pathname.startsWith(c.href))) {
        set.add(pillar.id);
      }
    }
    return set;
  });

  function togglePillar(id: string) {
    setExpandedPillars((prev) => {
      const next = new Set(prev);
      if (next.has(id)) next.delete(id);
      else next.add(id);
      return next;
    });
  }

  function isChildVisible(child: NavChild): boolean {
    if (!child.flag) return true;
    return !!orgProfile[child.flag];
  }
```

- [ ] **Step 5: Sostituisci il rendering della nav**

Trova il blocco `{/* Groups */}` nella nav (circa riga 251–293) e **sostituiscilo** con:

```tsx
            {/* 5 Pilastri */}
            {PILLARS.map((pillar) => {
              const isExpanded = expandedPillars.has(pillar.id);
              const isPillarActive = pillar.href
                ? pathname.startsWith(pillar.href)
                : pillar.children?.some((c) => pathname.startsWith(c.href)) ?? false;

              // Pilastro senza figli — link diretto
              if (!pillar.children) {
                return (
                  <Link
                    key={pillar.id}
                    href={pillar.href!}
                    onClick={() => setSidebarOpen(false)}
                    title={collapsed ? pillar.label : undefined}
                    className={`flex items-center ${collapsed ? "justify-center px-0 py-2" : "justify-between px-2 py-1.5"} rounded-md text-[11px] mb-0.5 transition-all`}
                    style={isPillarActive
                      ? { background: "rgba(255,255,255,0.12)", color: "#ffffff" }
                      : { color: "rgba(255,255,255,0.65)" }}
                  >
                    <div className={`flex items-center ${collapsed ? "" : "gap-2"}`}>
                      <pillar.icon className="h-3.5 w-3.5 flex-shrink-0" />
                      {!collapsed && <span>{pillar.label}</span>}
                    </div>
                    {!collapsed && pillar.art && (
                      <span className="text-[9px] px-1.5 py-0.5 rounded"
                        style={{ background: "rgba(255,255,255,0.1)", color: "rgba(255,255,255,0.55)" }}>
                        {pillar.badge === "urgent" ? "⚡" : pillar.art}
                      </span>
                    )}
                  </Link>
                );
              }

              // Pilastro con figli — accordion
              const visibleChildren = pillar.children.filter(isChildVisible);
              if (visibleChildren.length === 0) return null;

              return (
                <div key={pillar.id} className="mb-0.5">
                  {/* Header pilastro */}
                  <button
                    onClick={() => collapsed ? undefined : togglePillar(pillar.id)}
                    title={collapsed ? pillar.label : undefined}
                    className={`w-full flex items-center ${collapsed ? "justify-center px-0 py-2" : "justify-between px-2 py-1.5"} rounded-md text-[11px] transition-all`}
                    style={isPillarActive
                      ? { background: "rgba(255,255,255,0.08)", color: "#ffffff" }
                      : { color: "rgba(255,255,255,0.65)" }}
                  >
                    <div className={`flex items-center ${collapsed ? "" : "gap-2"}`}>
                      <pillar.icon className="h-3.5 w-3.5 flex-shrink-0" />
                      {!collapsed && <span className="font-medium">{pillar.label}</span>}
                    </div>
                    {!collapsed && (
                      <div className="flex items-center gap-1">
                        {pillar.art && (
                          <span className="text-[9px] px-1.5 py-0.5 rounded"
                            style={{ background: "rgba(255,255,255,0.1)", color: "rgba(255,255,255,0.55)" }}>
                            {pillar.art}
                          </span>
                        )}
                        <ChevronDown
                          className={`h-3 w-3 transition-transform ${isExpanded ? "rotate-180" : ""}`}
                          style={{ color: "rgba(255,255,255,0.4)" }}
                        />
                      </div>
                    )}
                  </button>

                  {/* Children — visibili solo se espanso (o collapsed → tooltip) */}
                  {(isExpanded || collapsed) && (
                    <div className={collapsed ? "" : "ml-3 mt-0.5 space-y-0.5"}>
                      {visibleChildren.map((child) => {
                        const isActive = pathname.startsWith(child.href);
                        return (
                          <Link
                            key={child.href}
                            href={child.href}
                            onClick={() => setSidebarOpen(false)}
                            title={collapsed ? child.label : undefined}
                            className={`flex items-center ${collapsed ? "justify-center px-0 py-1.5" : "justify-between px-2 py-1"} rounded-md text-[11px] transition-all`}
                            style={isActive
                              ? { background: "rgba(255,255,255,0.12)", color: "#ffffff" }
                              : { color: "rgba(255,255,255,0.5)" }}
                          >
                            <div className={`flex items-center ${collapsed ? "" : "gap-2"}`}>
                              {!collapsed && <div className="w-px h-3 rounded" style={{ background: "rgba(255,255,255,0.2)" }} />}
                              <child.icon className="h-3 w-3 flex-shrink-0" />
                              {!collapsed && <span>{child.label}</span>}
                            </div>
                            {!collapsed && child.art && (
                              <span className="text-[9px] px-1 py-0.5 rounded"
                                style={{ background: "rgba(255,255,255,0.08)", color: "rgba(255,255,255,0.4)" }}>
                                {child.art}
                              </span>
                            )}
                          </Link>
                        );
                      })}
                    </div>
                  )}
                </div>
              );
            })}

            {/* Settings — sempre visibile in fondo alla nav */}
            <div className="mt-4 pt-3" style={{ borderTop: "1px solid rgba(255,255,255,0.08)" }}>
              <Link
                href="/dashboard/account"
                onClick={() => setSidebarOpen(false)}
                title={collapsed ? "Settings" : undefined}
                className={`flex items-center ${collapsed ? "justify-center px-0 py-2" : "gap-2 px-2 py-1.5"} rounded-md text-[11px] transition-all`}
                style={pathname.startsWith("/dashboard/account")
                  ? { background: "rgba(255,255,255,0.12)", color: "#ffffff" }
                  : { color: "rgba(255,255,255,0.5)" }}
              >
                <Settings className="h-3.5 w-3.5" />
                {!collapsed && <span>Settings</span>}
              </Link>
            </div>
```

- [ ] **Step 6: Rimuovi il codice precedente di `filteredNavGroups` che ora non serve**

Rimuovi queste righe che fanno riferimento a `navGroups` e `ROLE_HIDDEN_HREFS` (non più usati):

```typescript
  const hiddenHrefs = role ? (ROLE_HIDDEN_HREFS[role] ?? []) : [];
  const filteredNavGroups = navGroups
    .map((group) => ({
      ...group,
      items: group.items.filter((item) => !hiddenHrefs.includes(item.href)),
    }))
    .filter((group) => group.items.length > 0);
```

E rimuovi anche le costanti `ROLE_HIDDEN_HREFS` e la vecchia `navGroups` (ora sostituita da `PILLARS`).

**Nota:** il filtro per ruolo viene rimosso in questa versione — la sidebar v2 usa i 5 pilastri universali con solo filtro condizionale per flag. Il role-based hiding può essere reintrodotto in futuro se necessario.

- [ ] **Step 7: Verifica TypeScript**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -15
```
Expected: nessun output (o solo warning pre-esistenti non correlati)

- [ ] **Step 8: Verifica visiva — avvia server**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && npm run dev -- --webpack &
```

Apri http://localhost:3000/dashboard e verifica:
- [ ] 5 pilastri visibili: Triage, Risk Manager, DocuGen AI, Data Audit, Compliance Ops + Settings
- [ ] Risk Manager e Compliance Ops si espandono/contraggono al click
- [ ] Auto-espansione quando navighi a una route figlia (es. `/dashboard/tools/risk-manager`)
- [ ] Collapsed mode: solo icone, tooltip al hover
- [ ] L.132 e AGID/ACN non visibili (paItaly=false by default)

- [ ] **Step 9: Commit**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && git add src/app/dashboard/layout.tsx && git commit -m "feat: sidebar v2 — 5 pilastri operativi con accordion e filtri condizionali"
```

---

## Task 4: Settings — geo-flag PA Italy + NIST opt-in

**Files:**
- Modify: `src/app/dashboard/account/page.tsx` (aggiungi sezione Settings organizzazione)

- [ ] **Step 1: Sostituisci il contenuto di account/page.tsx**

```tsx
// src/app/dashboard/account/page.tsx
"use client";

import { useOrgProfile } from "@/lib/hooks/useOrgProfile";

export default function AccountPage() {
  const { profile, setProfile } = useOrgProfile();

  return (
    <div className="max-w-xl mx-auto py-10 px-4 space-y-8">
      <div>
        <h1 className="mb-1" style={{ fontSize: "24px", fontWeight: 600, letterSpacing: "-0.5px", color: "#0D1016" }}>
          Settings
        </h1>
        <p className="text-[13px]" style={{ color: "rgba(0,0,0,0.45)" }}>
          Configurazione organizzazione e preferenze di compliance.
        </p>
      </div>

      {/* Profilo organizzazione */}
      <section>
        <h2 className="text-[13px] font-semibold mb-3" style={{ color: "#0D1016" }}>
          Profilo organizzazione
        </h2>
        <div className="rounded-xl divide-y" style={{ border: "1px solid rgba(0,0,0,0.1)" }}>
          <ToggleRow
            label="Opero con la PA italiana"
            description="Abilita sezioni L.132/2025 e AGID/ACN in Compliance Ops"
            checked={profile.paItaly}
            onChange={(v) => setProfile({ paItaly: v })}
          />
        </div>
      </section>

      {/* Integrazioni framework */}
      <section>
        <h2 className="text-[13px] font-semibold mb-3" style={{ color: "#0D1016" }}>
          Integrazioni framework
        </h2>
        <div className="rounded-xl divide-y" style={{ border: "1px solid rgba(0,0,0,0.1)" }}>
          <ToggleRow
            label="Abilita mapping NIST AI RMF"
            description="Aggiunge tab NIST (GOVERN/MAP/MEASURE/MANAGE) in DocuGen AI — Enterprise"
            checked={profile.nistEnabled}
            onChange={(v) => setProfile({ nistEnabled: v })}
            badge="Enterprise"
          />
        </div>
      </section>

      {/* Stato rilevamento GPAI */}
      {profile.gpaiDetected && (
        <section>
          <div
            className="rounded-xl p-4 flex items-start gap-3"
            style={{ background: "rgba(234,179,8,0.08)", border: "1px solid rgba(234,179,8,0.3)" }}
          >
            <span className="text-lg">🤖</span>
            <div>
              <p className="text-[13px] font-semibold" style={{ color: "#0D1016" }}>
                Sistema GPAI rilevato
              </p>
              <p className="text-[12px] mt-0.5" style={{ color: "rgba(0,0,0,0.55)" }}>
                Il Triage ha classificato il tuo sistema come modello di uso generale.
                DocuGen AI mostra il modulo GPAI Assessment (Art. 51-55).
              </p>
            </div>
          </div>
        </section>
      )}
    </div>
  );
}

function ToggleRow({
  label,
  description,
  checked,
  onChange,
  badge,
}: {
  label: string;
  description: string;
  checked: boolean;
  onChange: (v: boolean) => void;
  badge?: string;
}) {
  return (
    <div className="flex items-center justify-between px-4 py-3 gap-4">
      <div className="flex-1 min-w-0">
        <div className="flex items-center gap-2">
          <span className="text-[13px] font-medium" style={{ color: "#0D1016" }}>{label}</span>
          {badge && (
            <span className="text-[10px] px-1.5 py-0.5 rounded font-semibold"
              style={{ background: "rgba(99,102,241,0.1)", color: "#6366f1" }}>
              {badge}
            </span>
          )}
        </div>
        <p className="text-[11px] mt-0.5" style={{ color: "rgba(0,0,0,0.45)" }}>{description}</p>
      </div>
      <button
        role="switch"
        aria-checked={checked}
        onClick={() => onChange(!checked)}
        className="relative flex-shrink-0 w-9 h-5 rounded-full transition-colors"
        style={{ background: checked ? "#0D1016" : "rgba(0,0,0,0.12)" }}
      >
        <span
          className="absolute top-0.5 left-0.5 w-4 h-4 rounded-full bg-white transition-transform shadow-sm"
          style={{ transform: checked ? "translateX(16px)" : "translateX(0)" }}
        />
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Verifica TypeScript**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && npx tsc --noEmit 2>&1 | grep "account" | head -5
```
Expected: nessun output

- [ ] **Step 3: Test manuale**

1. Apri http://localhost:3000/dashboard/account
2. Attiva "Opero con la PA italiana" → verifica che L.132 e AGID/ACN compaiano in Compliance Ops sidebar
3. Disattiva → scompaiono
4. Attiva "Abilita mapping NIST AI RMF" → il valore viene salvato (ricarica pagina → toggle rimane attivo)

- [ ] **Step 4: Commit**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && git add src/app/dashboard/account/page.tsx && git commit -m "feat: settings page con toggle PA Italy e NIST opt-in"
```

---

## Task 5: DocuGen — step progress tracker + gate PDF

**Files:**
- Modify: `src/app/dashboard/tools/docugen/page.tsx` (aggiungere componente `DocuGenStepTracker` e modificare il bottone export)

Il file è 870 righe. La strategia è aggiungere un piccolo componente `DocuGenStepTracker` che:
1. Legge i `status` delle sezioni dal `DocuGenState.status` già in localStorage
2. Mappa le sezioni esistenti ai 9 step dello spec
3. Mostra un pannellino con checkbox
4. Ritorna `canExport: boolean` (true solo se art14 e art15 sono "done")

- [ ] **Step 1: Comprendi la struttura sezioni di DocuGen**

Prima di modificare, verifica i nomi delle sezioni nel file. Esegui:

```bash
grep -n '"section"\|sectionId\|SECTIONS\|section_id' "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply/src/app/dashboard/tools/docugen/page.tsx" | head -20
```

I nomi delle sezioni correnti (campo `status`) sono chiavi stringa come `"description"`, `"transparency"`, `"oversight"`, `"resilience"`, `"art50"` ecc. Se i nomi differiscono, adatta la mappa nel Step 3.

- [ ] **Step 2: Aggiungi interfaccia e costante step map dopo le import (riga ~15)**

```typescript
// ── DocuGen Step Tracker ──────────────────────────────────────────────────────

interface DocuGenStep {
  id: string;          // chiave in DocuGenState.status
  label: string;
  art: string;
  mandatory: boolean;  // se true, blocca export se non "done"
  auto?: boolean;      // step completati automaticamente
}

const DOCUGEN_STEPS: DocuGenStep[] = [
  { id: "description",  label: "01. Descrizione sistema",         art: "All. IV §1",  mandatory: false },
  { id: "purpose",      label: "02. Intended purpose + users",    art: "All. IV §2",  mandatory: false },
  { id: "transparency", label: "03. Art. 13 — Trasparenza",       art: "Art. 13",     mandatory: false },
  { id: "oversight",    label: "04. Art. 14 — Human Oversight",   art: "Art. 14",     mandatory: true  },
  { id: "resilience",   label: "05. Art. 15 — Resilienza",        art: "Art. 15",     mandatory: true  },
  { id: "art50",        label: "06. Art. 50 — Transparency limited", art: "Art. 50",  mandatory: false },
  { id: "techstack",    label: "07. Tech Stack",                  art: "All. IV §5",  mandatory: false, auto: true },
  { id: "metrics",      label: "08. Metriche performance",        art: "All. IV §6",  mandatory: false },
  { id: "bias",         label: "09. Bias mitigation",             art: "Art. 10",     mandatory: false },
];
```

- [ ] **Step 3: Aggiungi componente `DocuGenStepTracker` subito prima di `export default`**

```tsx
function DocuGenStepTracker({
  status,
  onStepClick,
}: {
  status: Record<string, "empty" | "draft" | "done">;
  onStepClick: (stepId: string) => void;
}) {
  const mandatory = DOCUGEN_STEPS.filter((s) => s.mandatory);
  const canExport = mandatory.every((s) => status[s.id] === "done");
  const doneCount = DOCUGEN_STEPS.filter((s) => status[s.id] === "done").length;

  return (
    <div className="rounded-xl overflow-hidden" style={{ border: "1px solid rgba(0,0,0,0.08)" }}>
      <div className="px-4 py-3 flex items-center justify-between" style={{ background: "rgba(0,0,0,0.02)", borderBottom: "1px solid rgba(0,0,0,0.06)" }}>
        <span className="text-[12px] font-semibold" style={{ color: "#0D1016" }}>
          Progresso DocuGen AI
        </span>
        <span className="text-[11px]" style={{ color: "rgba(0,0,0,0.4)" }}>
          {doneCount}/{DOCUGEN_STEPS.length}
        </span>
      </div>

      <div className="divide-y" style={{ divideColor: "rgba(0,0,0,0.06)" }}>
        {DOCUGEN_STEPS.map((step) => {
          const s = status[step.id] ?? "empty";
          const isDone = s === "done";
          const isDraft = s === "draft";

          return (
            <button
              key={step.id}
              onClick={() => onStepClick(step.id)}
              className="w-full flex items-center gap-2.5 px-4 py-2.5 text-left hover:bg-black/[0.02] transition-colors"
            >
              {/* Status indicator */}
              <div
                className="w-4 h-4 rounded-full flex-shrink-0 flex items-center justify-center"
                style={{
                  background: isDone ? "#0D1016" : isDraft ? "rgba(234,179,8,0.15)" : "rgba(0,0,0,0.06)",
                  border: isDone ? "none" : isDraft ? "1.5px solid rgba(234,179,8,0.6)" : "1.5px solid rgba(0,0,0,0.15)",
                }}
              >
                {isDone && (
                  <svg width="8" height="8" viewBox="0 0 8 8" fill="none">
                    <path d="M1.5 4L3.5 6L6.5 2" stroke="white" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
                  </svg>
                )}
                {isDraft && <div className="w-1.5 h-1.5 rounded-full" style={{ background: "rgba(234,179,8,0.8)" }} />}
              </div>

              {/* Label + art */}
              <div className="flex-1 min-w-0">
                <span className="text-[11px] block truncate" style={{ color: isDone ? "#0D1016" : "rgba(0,0,0,0.55)" }}>
                  {step.label}
                </span>
              </div>

              {/* Tags */}
              <div className="flex gap-1 flex-shrink-0">
                {step.mandatory && !isDone && (
                  <span className="text-[9px] px-1 py-0.5 rounded"
                    style={{ background: "rgba(220,38,38,0.08)", color: "#dc2626", fontWeight: 600 }}>
                    OBB
                  </span>
                )}
                {step.auto && (
                  <span className="text-[9px] px-1 py-0.5 rounded"
                    style={{ background: "rgba(0,0,0,0.06)", color: "rgba(0,0,0,0.35)" }}>
                    AUTO
                  </span>
                )}
                <span className="text-[9px] px-1 py-0.5 rounded"
                  style={{ background: "rgba(0,0,0,0.06)", color: "rgba(0,0,0,0.35)" }}>
                  {step.art}
                </span>
              </div>
            </button>
          );
        })}
      </div>

      {/* Export gate indicator */}
      {!canExport && (
        <div className="px-4 py-2.5 flex items-center gap-2" style={{ background: "rgba(220,38,38,0.04)", borderTop: "1px solid rgba(220,38,38,0.1)" }}>
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
            <circle cx="6" cy="6" r="5.5" stroke="#dc2626" strokeOpacity="0.5"/>
            <path d="M6 4v3M6 8.5v.5" stroke="#dc2626" strokeWidth="1.2" strokeLinecap="round"/>
          </svg>
          <span className="text-[10px]" style={{ color: "#dc2626" }}>
            Completa Art. 14 e Art. 15 per sbloccare l'export PDF
          </span>
        </div>
      )}
    </div>
  );
}
```

Il componente `DocuGenStepTracker` ritorna `canExport` tramite il calcolo interno — ma per il gate sul bottone PDF dobbiamo esporre questo valore. Modifichiamo l'approccio: calcoliamo `canExport` direttamente nel componente principale.

- [ ] **Step 4: Aggiungi calcolo `canExport` nel componente principale DocuGen**

Nel componente principale `DocuGenPage` (o come si chiama `export default function`), cerca dove viene calcolata la possibilità di esportare PDF (cerca `Download` o `pdf` o `export` nelle righe 400-870).

Aggiungi questa riga vicino alle variabili di stato:

```typescript
  const canExport = useMemo(() => {
    const mandatoryIds = DOCUGEN_STEPS.filter((s) => s.mandatory).map((s) => s.id);
    return mandatoryIds.every((id) => (state.status[id] ?? "empty") === "done");
  }, [state.status]);
```

- [ ] **Step 5: Trova il bottone Download/Export e aggiungi la condizione `disabled`**

Cerca nel file `Download` (l'icona) e trova il bottone di export. Aggiungi `disabled={!canExport}` e lo stile visivo:

```tsx
// Prima (esempio — il codice esatto varia):
<button onClick={handleExportPDF} className="...">
  <Download ... />
  Genera PDF
</button>

// Dopo:
<button
  onClick={handleExportPDF}
  disabled={!canExport}
  title={!canExport ? "Completa Art. 14 e Art. 15 prima di esportare" : undefined}
  className="... disabled:opacity-40 disabled:cursor-not-allowed"
>
  <Download ... />
  Genera PDF
</button>
```

- [ ] **Step 6: Aggiungi il `<DocuGenStepTracker>` nel JSX**

Trova un punto nella sidebar o nel layout di DocuGen (generalmente c'è una colonna laterale o una sezione di stato). Inserisci il tracker:

```tsx
<DocuGenStepTracker
  status={state.status}
  onStepClick={(stepId) => {
    // Scroll o focus sulla sezione corrispondente
    const el = document.getElementById(`section-${stepId}`);
    el?.scrollIntoView({ behavior: "smooth", block: "start" });
  }}
/>
```

Se il DocuGen non ha una sidebar, aggiungi il tracker come card in cima alla pagina, sopra il form principale.

- [ ] **Step 7: Verifica TypeScript**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && npx tsc --noEmit 2>&1 | grep "docugen" | head -10
```
Expected: nessun output

- [ ] **Step 8: Test manuale**

1. Apri http://localhost:3000/dashboard/tools/docugen
2. Verifica il tracker con 9 step (tutti vuoti inizialmente)
3. Compila una sezione → lo step relativo diventa "draft" o "done"
4. Verifica che il bottone "Genera PDF" sia disabilitato con tooltip
5. Completa Art. 14 e Art. 15 → bottone si sblocca

- [ ] **Step 9: Commit finale**

```bash
cd "/Users/umbertomottola/Desktop/open code - ai act saas/aicomply" && git add src/app/dashboard/tools/docugen/page.tsx && git commit -m "feat: DocuGen step tracker con 9 step + gate PDF su Art.14/15 obbligatori"
```

---

## Self-Review

### 1. Spec coverage

| Requisito spec | Task |
|----------------|------|
| 5 pilastri: Triage, Risk Manager, DocuGen, Data Audit, Compliance Ops | Task 3 |
| Risk Manager con sotto-voci: Risk Register, FRIA, DPIA | Task 3 |
| Compliance Ops con 7+ sotto-voci | Task 3 |
| Settings con geo-flag e NIST opt-in | Task 4 |
| Geo-filter PA Italy → L.132 + AGID/ACN | Task 1 + Task 3 |
| GPAI filter → GPAI Assessment in DocuGen | Task 1 + Task 2 |
| NIST AI RMF nascosto by default, opt-in | Task 4 |
| DocuGen progress 9 step con stati ✅/🟡/⬜ | Task 5 |
| Export PDF bloccato finché Art.14 e Art.15 non ✅ | Task 5 |
| AIA Architect rimosso dalla nav | Task 3 (non presente in PILLARS) |
| Transparency/Oversight/Resilience → step interni | Task 5 (mappa step) |

### 2. Placeholder scan

Nessun placeholder TBD/TODO/FIXME. Il Task 5 Step 5 richiede trovare il bottone Download nel file — se la struttura differisce, adattare in base al codice effettivo. Il tracker `DocuGenStepTracker` ha l'`onStepClick` col scroll che potrebbe non trovare `section-${stepId}` se i section ID nel form non coincidono — in quel caso il click non fa nulla (graceful degradation, non blocca la funzionalità).

### 3. Type consistency

- `OrgProfile` definito in Task 1, usato in Task 2 (useOrgProfile hook), Task 3 (sidebar), Task 4 (settings page) — consistente
- `DocuGenStep` definito in Task 5 Step 2, usato nel componente `DocuGenStepTracker` Task 5 Step 3 e nel `canExport` Task 5 Step 4 — consistente
- `NavPillar.children` usa tipo `NavChild` (Task 3 Step 1) — flag è `"paItaly" | "gpaiDetected" | "nistEnabled"` che corrisponde esattamente ai campi di `OrgProfile` — consistente
